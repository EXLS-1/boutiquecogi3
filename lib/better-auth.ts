// lib/better-auth.ts (deprecated)
// Doubles BetterAuth instance in the codebase.
// Keep this file for backward compatibility, but re-export the single source of truth.

export { auth } from "@/lib/auth";


